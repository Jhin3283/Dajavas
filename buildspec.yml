version: 0.2

phases:
  pre_build:
    commands:
      - cd client
      - npm install
  build:
    commands:
      - CI=npm run build

artifacts:
  files:
    - "**/*"
  base-directory: client/build
